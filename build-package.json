{
  "name": "textrack-build",
  "version": "1.0.0",
  "description": "TexTrack build tooling",
  "scripts": {
    "build": "node build.js",
    "install-build": "npm install terser html-minifier-terser"
  },
  "devDependencies": {
    "terser": "^5.30.0",
    "html-minifier-terser": "^7.2.0"
  }
}
